from setuptools import setup, find_packages

setup(
    name='currency_converter',
    version='0.1',
    packages=find_packages(),
    description='A simple currency converter library',
    author='Jules',
    author_email='jules@example.com',
)
